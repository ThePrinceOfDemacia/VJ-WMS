<UserControl x:Class="VjWms.Desktop.UI.Views.Pages.Transfers.TransferListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="🔄 Luân Chuyển Kho" FontSize="24" FontWeight="Bold"
                           Foreground="{DynamicResource TextPrimaryBrush}" />
                <TextBlock Text="Quản lý phiếu luân chuyển nội bộ giữa các kho" FontSize="14"
                           Foreground="{DynamicResource TextSecondaryBrush}" />
            </StackPanel>
            <Button Grid.Column="1" Content="➕  Tạo phiếu mới" Command="{Binding CreateNewCommand}"
                    Padding="16,10" FontSize="13" FontWeight="SemiBold" Cursor="Hand"
                    Background="{DynamicResource PrimaryBrush}" Foreground="White"
                    BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>

        <!-- Filter Bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Lọc:" Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0" />
            <ComboBox SelectedValue="{Binding FilterStatus}" SelectedValuePath="Tag"
                      Width="160" FontSize="13">
                <ComboBoxItem Content="Tất cả" Tag="All" IsSelected="True" />
                <ComboBoxItem Content="Nháp" Tag="Draft" />
                <ComboBoxItem Content="Chờ đồng bộ" Tag="PendingSync" />
                <ComboBoxItem Content="Bị từ chối" Tag="Rejected" />
            </ComboBox>
            <Button Content="🔄 Làm mới" Command="{Binding RefreshCommand}" Margin="12,0,0,0"
                    Padding="10,6" FontSize="12" Background="#1E293B" Foreground="#94A3B8"
                    BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- DataGrid -->
        <Border Grid.Row="2" Background="{DynamicResource BgCardBrush}" CornerRadius="12" Padding="4">
            <DataGrid ItemsSource="{Binding Transfers}" AutoGenerateColumns="False"
                      IsReadOnly="True" CanUserAddRows="False"
                      Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}"
                      BorderThickness="0" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#1E293B"
                      RowBackground="#0F172A" AlternatingRowBackground="#131C2E"
                      FontSize="13" HeadersVisibility="Column"
                      SelectionMode="Single" SelectionUnit="FullRow"
                      PreviewMouseLeftButtonDown="Grid_PreviewMouseLeftButtonDown">
                <DataGrid.InputBindings>
                    <MouseBinding Gesture="LeftDoubleClick" 
                                  Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                  CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                </DataGrid.InputBindings>
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1E293B" />
                        <Setter Property="Foreground" Value="#94A3B8" />
                        <Setter Property="Padding" Value="12,8" />
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                        <Setter Property="BorderThickness" Value="0" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,8" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Padding="{TemplateBinding Padding}"
                                            Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Số phiếu" Binding="{Binding LocalNumber}" Width="180" />
                    <DataGridTextColumn Header="Kho nguồn" Binding="{Binding SourceWarehouseName}" Width="180" />
                    <DataGridTextColumn Header="Kho đích" Binding="{Binding DestWarehouseName}" Width="180" />
                    <DataGridTextColumn Header="Loại" Binding="{Binding TransferType}" Width="100" />
                    <DataGridTextColumn Header="Ngày" Binding="{Binding TransferDate}" Width="110" />
                    <DataGridTextColumn Header="Dòng" Binding="{Binding ItemCount}" Width="60" />
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="120" />
                    <DataGridTemplateColumn Header="Thao tác" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="📤" Command="{Binding DataContext.SyncCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" ToolTip="Đồng bộ"
                                            Background="Transparent" Foreground="#10B981" BorderThickness="0"
                                            FontSize="14" Cursor="Hand" Padding="4,2"
                                            Visibility="{Binding CanSync, Converter={StaticResource BoolToVisConverter}}" />
                                    <Button Content="✏️" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" ToolTip="Sửa"
                                            Background="Transparent" Foreground="#F59E0B" BorderThickness="0"
                                            FontSize="14" Cursor="Hand" Padding="4,2"
                                            Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisConverter}}" />
                                    <Button Content="🗑️" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" ToolTip="Xóa"
                                            Background="Transparent" Foreground="#EF4444" BorderThickness="0"
                                            FontSize="14" Cursor="Hand" Padding="4,2"
                                            Visibility="{Binding CanEdit, Converter={StaticResource BoolToVisConverter}}" />
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Empty state -->
        <TextBlock Grid.Row="2" Text="Chưa có phiếu luân chuyển nào. Nhấn 'Tạo phiếu mới' để bắt đầu!"
                   FontSize="14" Foreground="#475569"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding Transfers.Count, Converter={StaticResource ZeroToVisibleConverter}, FallbackValue=Visible}" />
    </Grid>
</UserControl>
